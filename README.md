# Typescript SDK

This is a small sandbox for testing the [Typescript Sharetribe SDK pakage](https://www.npmjs.com/package/@vansite/ts-sharetribe-flex-sdk)

## Usage
Plug in client IDs, secrets, and other required info. Toggle `isMarketplaceAPI` to use different data sets.
